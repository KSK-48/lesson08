const link = document.querySelector('a')
console.log(link.textContent)

// todoの中からh1タグを探していく
const title = todo.querySelector('h1')
console.log(title.textContent)

const list = todo.querySelector('.list')
console.log(list.children[1].textContent)